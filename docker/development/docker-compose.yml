services:
  dms_kiosk:
    build:
      context: ../../
      dockerfile: docker/development/Dockerfile
    command: "node server.js"
    environment:
      - PORT=4000
      - NODE_ENV=development
    env_file:
      - ../../.env
    ports:
      - "4000:4000"
